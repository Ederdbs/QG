% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_mabc.R
\name{make_shared_ibd}
\alias{make_shared_ibd}
\title{Genome shared by descent between donor and elite}
\usage{
make_shared_ibd(
  map,
  s,
  mw,
  block_cM = 25,
  target_idx = NULL,
  clear_cM = 10,
  tol = 0.01,
  max_try = 300
)
}
\arguments{
\item{map}{A map from [build_map].}

\item{s}{Target shared fraction, in [0, 1).}

\item{mw}{Marker weights, see [marker_weights].}

\item{block_cM}{Mean block length in cM.}

\item{target_idx}{Marker indices of the targets, see [target_indices].}

\item{clear_cM}{Half-width in cM of the non-shared window around each target.}

\item{tol}{Tolerance on the realised fraction.}

\item{max_try}{Rejection-sampling attempts; the best draw is returned.}
}
\value{
A logical vector of length `map$m`, TRUE where the genome is shared.
}
\description{
Donor and elite are both maize and often both from the same heterotic group,
so a fraction `s` of the genome is already identical by descent. Donor genome
inside a shared tract costs nothing, which is why `s` has to be modelled
explicitly. Sharing is drawn as a two-state Markov process in blocks and
then rejection-sampled so the realised fraction matches `s`.
}
\details{
Every target is forced into a **non**-shared region: a target that were
already shared would not need introgressing.

*Limitation:* real donor-elite sharing is structured by heterotic group, with
long tracts between related lines and near-zero sharing for exotic donors.
Forcing `s` to be exact also removes legitimate pair-to-pair variance.
}
