% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_search.R
\name{sel_local_search}
\alias{sel_local_search}
\title{Local search over the swap neighbourhood}
\usage{
sel_local_search(
  ctx,
  n_sel,
  alpha_max,
  gd_ref,
  start = NULL,
  max_use = NULL,
  mode = c("hill", "anneal", "tempering"),
  iters = 20000,
  n_chains = 8,
  penalty = 5,
  t0 = NULL,
  swap_every = 50L,
  seed = NULL,
  trace = FALSE
)
}
\arguments{
\item{ctx}{Context augmented by [fast_ctx].}

\item{n_sel}{Number of hybrids to select.}

\item{alpha_max}{Maximum acceptable relative loss of gene diversity.}

\item{gd_ref}{Reference gene diversity from [null_distribution].}

\item{start}{Integer vector to start from. `NULL` (the default) builds a
warm start, exactly as `sel_de(seeds = NULL)` does; it never means "start
cold".}

\item{max_use}{Maximum times any line may be used, or `NULL` for no cap.}

\item{mode}{`"hill"`, `"anneal"` or `"tempering"`.}

\item{iters}{Total proposed moves, summed over chains, so the wall-clock
cost of the three modes is comparable at equal `iters`.}

\item{n_chains}{Number of replicas when `mode = "tempering"`.}

\item{penalty}{Penalty multiplier on the diversity violation.}

\item{t0}{Starting temperature. `NULL` calibrates it from the median
absolute objective change over a short burn-in of random moves, which
avoids a magic constant that would not transfer between datasets.}

\item{swap_every}{Replica-exchange interval, in moves per chain.}

\item{seed}{Optional integer for reproducibility.}

\item{trace}{Record an anytime trace of `(evals, seconds, best)`.}
}
\value{
An integer vector of selected hybrid indices, carrying attributes
  `objective` and, when `trace = TRUE`, `trace`.
}
\description{
The search this package's own primitives were written for.
[theta_state]/[theta_swap] maintain group coancestry in `O(L)` per move, so
a swap costs about 5 microseconds at production scale against 85 for the
`f`-submatrix route -- and no `N x N` matrix is formed at all.
}
\details{
Three modes, one driver, differing only in the temperature schedule:
`"hill"` accepts improving moves only, `"anneal"` runs one geometrically
cooled Metropolis chain, and `"tempering"` runs `n_chains` chains on a
temperature ladder with periodic replica exchange. De Beukelaer et al. (2018) found
parallel tempering to win on this class of problem not by being individually
cleverer but by combining many simple local searches.

The per-line cap is a **partition matroid** and is enforced as a hard
feasibility test on each move. The diversity budget is a proportional
penalty, matching [make_fitness_fast].
}
\examples{
cfg <- modifyList(sim_config, list(n_pool_A = 8, n_pool_B = 8, m = 200))
ctx <- fast_ctx(simulate_data(cfg))
nl <- null_distribution(ctx, 12, B = 50, B_full = 10)
s <- sel_local_search(ctx, 12, 0.01, nl$gd_ref, iters = 500, seed = 1)
length(s)
}
\seealso{
[sel_de] for the population alternative, [ocs_round] for a bound.

Other selection strategies:
\code{\link[=ocs_round]{ocs_round()}},
\code{\link[=sel_de]{sel_de()}},
\code{\link[=sel_de_fast]{sel_de_fast()}},
\code{\link[=sel_exact]{sel_exact()}},
\code{\link[=sel_greedy]{sel_greedy()}},
\code{\link[=sel_random]{sel_random()}},
\code{\link[=sel_truncation]{sel_truncation()}},
\code{\link[=sel_truncation_cap]{sel_truncation_cap()}}
}
\concept{selection strategies}
