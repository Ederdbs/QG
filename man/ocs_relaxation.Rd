% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_de_select.R
\name{ocs_relaxation}
\alias{ocs_relaxation}
\title{Continuous optimal-contribution relaxation}
\usage{
ocs_relaxation(ctx, n_sel, lambdas, iters = 4000, lines = NULL)
}
\arguments{
\item{ctx}{Evaluation context, see [build_ctx].}

\item{n_sel}{Number of hybrids, which sets the contribution cap.}

\item{lambdas}{Numeric vector of diversity weights to sweep.}

\item{iters}{Projected-gradient iterations per lambda.}

\item{lines}{Use the line route for the matrix-vector products, which is
exact and costs O(N + L^2) instead of O(N^2). `NULL` auto-selects it when
the hybrid matrix is absent, so results computed with an `N x N` `f`
present are unchanged.}
}
\value{
A matrix with one row per lambda.
}
\description{
Maximises `c'u - lambda c'f c` subject to `sum(c) = 1` and
`0 <= c <= 1 / n_sel`. The cap is what makes this a valid **upper bound**
for the equal-weight 0/1 problem: a large gap against [sel_de] means the DE
has not converged.
}
\details{
Too few iterations yield an "upper bound" smaller than the discrete optimum,
which is useless. The default is the minimum that converged in testing;
re-check it whenever the dataset changes.
}
