% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/00_data.R
\name{vanraden_G}
\alias{vanraden_G}
\title{VanRaden genomic relationship matrix}
\usage{
vanraden_G(M)
}
\arguments{
\item{M}{Marker matrix, individuals in rows, dosage coded 0/1/2.}
}
\value{
A list with `G` (the relationship matrix), `Z` (the centered marker
  matrix) and `p` (the allele frequencies used for centering).
}
\description{
`G = ZZ' / (2 sum p(1 - p))` with `Z` centered on the population's own allele
frequencies.
}
\details{
Use this for trait prediction only. It cannot measure diversity: because `Z`
is centered on its own population, `sum(G) == 0` exactly, so group coancestry
and any status number derived from it are meaningless.
}
\seealso{
[molecular_coancestry] for the diversity counterpart.
}
