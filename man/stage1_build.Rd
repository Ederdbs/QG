% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/10_stage1.R
\name{stage1_build}
\alias{stage1_build}
\title{Stage 1: from line genotypes to the hybrid contract}
\usage{
stage1_build(X, ped, traits, fL = NULL)
}
\arguments{
\item{X}{Hybrid marker matrix, `N x m`. Accepts 0/1/2 or 0/0.5/1 coding; the
coding is auto-detected and rescaled.}

\item{ped}{Data frame with columns `a` and `b`, the parent line ids, one row
per hybrid.}

\item{traits}{Data frame or matrix of predicted trait values, one row per
hybrid.}

\item{fL}{Optional coancestry matrix *between lines*. Stage 2 runs without
it; it only enables the heterotic-group decomposition
([theta_pools], [gd_partition]).}
}
\value{
A list with `X` (markers in 0/0.5/1), `f` (hybrid molecular
  coancestry), `hybrids` (id, parent lines, traits) and `fL`.
}
\description{
The seam between the simulation and the rest of the pipeline. Everything
downstream talks only through the objects returned here, which is what makes
swapping in real data a single-function change.
}
\seealso{
[stage2_select] for the next stage.
}
