% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/04_fast_metrics.R
\name{make_fitness_fast}
\alias{make_fitness_fast}
\title{Differential-evolution fitness on the line route}
\usage{
make_fitness_fast(
  ctx,
  n_sel,
  gd_ref,
  alpha_max,
  ne_par_min = NULL,
  max_use = NULL,
  pool_tol = NULL,
  index = ctx$index,
  penalty = 5,
  encoding = c("lines", "hybrid")
)
}
\arguments{
\item{ctx}{Context augmented by [fast_ctx].}

\item{n_sel}{Number of hybrids to select.}

\item{gd_ref}{Reference gene diversity from [null_distribution].}

\item{alpha_max}{Maximum acceptable relative loss of gene diversity.}

\item{ne_par_min}{Minimum effective number of parents, or `NULL`.}

\item{max_use}{Maximum use of any single line, or `NULL`.}

\item{pool_tol}{Tolerance on the deviation of pool balance from 0.5, or
`NULL`.}

\item{index}{The index to maximise.}

\item{penalty}{Penalty multiplier on the total constraint violation.}

\item{encoding}{`"lines"` (recommended, dimension `L`) or `"hybrid"`
(dimension `N`).}
}
\value{
A function of the DE parameter vector returning a value to minimise.
}
\description{
The drop-in replacement for [make_fitness] at production scale. Uses only
`ctx$fL` and `ctx$parents` in the inner loop: no `N x N` matrix is ever
formed or indexed. Handles several simultaneous restrictions, each entering
as a proportional penalty.
}
