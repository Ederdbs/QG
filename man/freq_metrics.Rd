% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_metrics.R
\name{freq_metrics}
\alias{freq_metrics}
\title{Frequency-based metrics, in a single marker sweep}
\usage{
freq_metrics(idx, ctx, maf = 0.05, rare_lo = 0.01, eps = 1e-06)
}
\arguments{
\item{idx}{Integer vector of selected row indices.}

\item{ctx}{Evaluation context, see [build_ctx].}

\item{maf}{Minor allele frequency threshold for `alleles_lost`.}

\item{rare_lo}{Lower bound defining the "rare in the pool" marker class.}

\item{eps}{Tolerance for dropping markers fixed in the base.}
}
\value{
A named numeric vector `He`, `alleles_lost`, `F_hom`, `F_drift`,
  `cov_diag`, `cor_dp_p0`, `rare_retained`.
}
\description{
Everything that needs a pass over the markers, in **one** pass over
`X[idx, ]`. Anything new that needs `colMeans(X[idx, ])` belongs here, not in
a new function with its own sweep.
}
\details{
Returns the two lenses of Meuwissen et al. (2020): `F_hom` sees
heterozygosity lost, `F_drift` sees allele-frequency change. Constraining
theta (their `G_0.5` scheme) pins `F_hom` and leaves `F_drift` free -- that is
the unmonitored bill. `cov_diag` is exactly the gap between them (their
Eq. 3). Both are anchored on `ctx$p0`, the frozen candidate-pool
frequencies; never recompute a base per scenario.
}
\seealso{
Other diversity metrics:
\code{\link[=alleles_lost]{alleles_lost()}},
\code{\link[=ane]{ane()}},
\code{\link[=eff_dim]{eff_dim()}},
\code{\link[=ene]{ene()}},
\code{\link[=gd_partition]{gd_partition()}},
\code{\link[=gene_diversity]{gene_diversity()}},
\code{\link[=gst]{gst()}},
\code{\link[=he_nei]{he_nei()}},
\code{\link[=max_line_use]{max_line_use()}},
\code{\link[=mean_offdiag]{mean_offdiag()}},
\code{\link[=ne_lines_pool]{ne_lines_pool()}},
\code{\link[=ne_parents]{ne_parents()}},
\code{\link[=status_number]{status_number()}},
\code{\link[=theta_from_freq]{theta_from_freq()}},
\code{\link[=theta_group]{theta_group()}},
\code{\link[=theta_pools]{theta_pools()}}
}
\concept{diversity metrics}
