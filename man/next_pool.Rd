% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_cycles.R
\name{next_pool}
\alias{next_pool}
\title{Next generation of one pool, by weighted intermating and DH derivation}
\usage{
next_pool(GL_pool, w, n_out)
}
\arguments{
\item{GL_pool}{Line matrix for one pool, `n x m`, coded 0/1.}

\item{w}{Usage weights, length `nrow(GL_pool)`, need not sum to 1.}

\item{n_out}{Number of lines to produce.}
}
\value{
A matrix `n_out x m`, coded 0/1.
}
\description{
Parents are drawn from `GL_pool` with probability proportional to `w`, paired
at random, and one doubled haploid is derived per pair. The lines are inbred,
so `(GL[i, ] + GL[j, ]) / 2` is 0, 0.5 or 1, and a single Bernoulli draw
per locus is an exact DH gamete under free recombination. A pair that draws
the same line twice returns that line, which is correct: selfing an inbred
reproduces it.
}
\seealso{
[run_cycles], which calls this once per pool per cycle.
}
