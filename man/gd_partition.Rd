% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/01_metrics.R
\name{gd_partition}
\alias{gd_partition}
\title{Caballero & Toro partition of gene diversity}
\usage{
gd_partition(idx, ctx, tp = theta_pools(idx, ctx))
}
\arguments{
\item{idx}{Integer vector of selected row indices.}

\item{ctx}{Evaluation context, see [build_ctx].}

\item{tp}{Pool coancestries, see [theta_pools]. Passed in to avoid
recomputing them.}
}
\value{
A named numeric vector `GD_WI_hyb`, `GD_T`, `GD_WI`, `GD_BI`,
  `GD_BS`, `F_ST`.
}
\description{
Equation 8 of Caballero & Toro (2002):
`GD_T = GD_WI + GD_BI + GD_BS`, with `F_ST = GD_BS / GD_T`.
}
\details{
Subpopulations are the two heterotic pools, weighted by the line usage the
selection induces. `GD_BS` is the between-pool divergence -- the heterosis
engine, the one component that must be *preserved* rather than minimised.
With inbred parent lines `GD_WI` is 0 by construction (all diversity is
`BI + BS`); the hybrid-level `GD_WI_hyb` is the component that carries signal
and it needs no `fL`, so it is always returned.
}
\seealso{
Other diversity metrics:
\code{\link[=alleles_lost]{alleles_lost()}},
\code{\link[=ane]{ane()}},
\code{\link[=eff_dim]{eff_dim()}},
\code{\link[=ene]{ene()}},
\code{\link[=freq_metrics]{freq_metrics()}},
\code{\link[=gene_diversity]{gene_diversity()}},
\code{\link[=gst]{gst()}},
\code{\link[=he_nei]{he_nei()}},
\code{\link[=max_line_use]{max_line_use()}},
\code{\link[=mean_offdiag]{mean_offdiag()}},
\code{\link[=ne_lines_pool]{ne_lines_pool()}},
\code{\link[=ne_parents]{ne_parents()}},
\code{\link[=status_number]{status_number()}},
\code{\link[=theta_from_freq]{theta_from_freq()}},
\code{\link[=theta_group]{theta_group()}},
\code{\link[=theta_pools]{theta_pools()}}
}
\concept{diversity metrics}
