% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_mabc.R
\name{sizing_table}
\alias{sizing_table}
\title{Analytical sizing by stage}
\usage{
sizing_table(k_max = 4, flank_cM = 5, P = 0.95, n_keep = 3)
}
\arguments{
\item{k_max}{Largest number of genes.}

\item{flank_cM}{Flanking-marker distance for the recombinant column.}

\item{P}{Probability at which a size is declared sufficient.}

\item{n_keep}{Individuals required at the fixation stage.}
}
\value{
A data.frame, one row per number of genes.
}
\description{
Population sizes for each stage of a conversion programme, for 1 to `k_max`
genes: the backcross foreground, the backcross foreground plus one
recombinant in a flanking interval, fixation by selfing, and fixation by
doubled haploids.
}
\details{
The DH column is the one that matters in maize. A DH line derived from a
BCnF1 gamete is homozygous at every locus, so it carries all `k` targets with
probability `(1/2)^k` rather than `(1/4)^k` -- 99 lines against 1610 plants
at four genes.
}
\examples{
sizing_table()
}
