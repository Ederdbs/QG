% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/03_de_select.R
\name{sel_de}
\alias{sel_de}
\title{Differential-evolution selection under a diversity constraint}
\usage{
sel_de(
  ctx,
  n_sel,
  alpha_max,
  gd_ref,
  NP = 300,
  itermax = 2000,
  seeds = NULL,
  trace = FALSE,
  max_use = NULL
)
}
\arguments{
\item{ctx}{Evaluation context, see [build_ctx].}

\item{n_sel}{Number of hybrids to select.}

\item{alpha_max}{Maximum acceptable relative loss of gene diversity.}

\item{gd_ref}{Reference gene diversity from [null_distribution].}

\item{NP}{Population size.}

\item{itermax}{Maximum generations.}

\item{seeds}{A list of index vectors to seed the initial population.
`NULL` (the default) builds a greedy sweep automatically. Passing a single
random vector is how the book demonstrates what a cold start costs; do not
do it in production.}

\item{trace}{Passed to `DEoptim` for progress reporting.}

\item{max_use}{Maximum times any line may be used, or `NULL` for no cap.
The default is `NULL`, which reproduces the behaviour this function had
before the cap existed. Set it to make the per-line usage restriction --
a partition matroid -- active on the production route; the violation
enters proportionally, as the diversity budget does.}
}
\value{
An integer vector of selected hybrid indices.
}
\description{
Runs `DEoptim` on the continuous encoding of [decode], with the fitness from
[make_fitness].
}
\details{
The population **must** be seeded with greedy solutions. Without that warm
start, 4.5e5 evaluations are not enough to beat greedy on roughly 100
integer dimensions.
}
\seealso{
Other selection strategies:
\code{\link[=ocs_round]{ocs_round()}},
\code{\link[=sel_de_fast]{sel_de_fast()}},
\code{\link[=sel_exact]{sel_exact()}},
\code{\link[=sel_greedy]{sel_greedy()}},
\code{\link[=sel_local_search]{sel_local_search()}},
\code{\link[=sel_random]{sel_random()}},
\code{\link[=sel_truncation]{sel_truncation()}},
\code{\link[=sel_truncation_cap]{sel_truncation_cap()}}
}
\concept{selection strategies}
