% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/11_stage2.R
\name{stage2_select}
\alias{stage2_select}
\title{Stage 2: constrained selection across a grid of diversity budgets}
\usage{
stage2_select(
  X,
  f,
  hybrids,
  n_sel,
  alphas = NULL,
  weights = NULL,
  fL = NULL,
  include_references = TRUE,
  B_null = 2000,
  B_full = 200,
  NP = 300,
  itermax = 2000,
  seed = 1,
  verbose = TRUE
)
}
\arguments{
\item{X}{Hybrid marker matrix from [stage1_build].}

\item{f}{Hybrid molecular coancestry matrix from [stage1_build].}

\item{hybrids}{Hybrid table from [stage1_build].}

\item{n_sel}{Number of hybrids to select.}

\item{alphas}{Numeric vector of diversity-loss budgets, or `NULL` for an
automatic grid up to the attainable ceiling.}

\item{weights}{Trait weights for the index, or `NULL` for equal weights.}

\item{fL}{Optional line coancestry matrix; enables the heterotic-group
decomposition.}

\item{include_references}{Also evaluate random, truncation and
maximum-diversity references.}

\item{B_null, B_full}{Replicates for the cheap and full null panels.}

\item{NP, itermax}{Differential-evolution population size and generations.}

\item{seed}{Random seed.}

\item{verbose}{Print progress.}
}
\value{
A list with `selection` (0/1 columns per scenario), `metrics` (one
  row per scenario), `z_scores` (each metric in standard deviations from the
  random null) and `ref` (`gd_ref`, its Monte Carlo standard error,
  `alpha_max`, `n_sel`).
}
\description{
Runs the full selection pipeline: the random-subset null distribution, the
attainable `alpha` ceiling implied by truncation, a greedy warm start and
differential evolution for each `alpha` scenario, then the full metric panel
and its z-scores against the null.
}
\details{
Every metric is reported against the random null, because a metric without a
baseline says nothing.
}
\seealso{
[stage1_build] for the previous stage.
}
