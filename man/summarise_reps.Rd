% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_mabc.R
\name{summarise_reps}
\alias{summarise_reps}
\title{Summarise replicate backcross programmes}
\usage{
summarise_reps(A, opt, k)
}
\arguments{
\item{A}{A data.frame of stacked `run_bc_program()$traj`, with a `rep` column.}

\item{opt}{Options, see [mabc_opt].}

\item{k}{Number of genes, used for the drag ceiling `drag_per_gene * k`.}
}
\value{
A one-row data.frame.
}
\description{
Collapses the per-replicate trajectories of one cell (one `N`, one gene
number, one `s`) into a single row. Replicates that lost the targets count
as failures in every probability, which is why `p_ibd` and `p_drag` are
divided by the number of replicates and not by the number of survivors.
}
