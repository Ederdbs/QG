% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_cycles.R
\name{run_cycles}
\alias{run_cycles}
\title{Run recurrent hybrid selection for several cycles}
\usage{
run_cycles(
  n_A = 25,
  n_B = 25,
  m = 2000,
  n_cycles = 10,
  n_sel = 60,
  alpha_max = 0.01,
  n_qtl = 300,
  fst = 0.15,
  h2 = 0.5,
  max_use = NULL,
  selector = NULL,
  inject = NULL,
  B_null = 200,
  seed = 1
)
}
\arguments{
\item{n_A, n_B}{Lines per pool, held constant across cycles.}

\item{m}{Markers.}

\item{n_cycles}{Number of cycles to run after the founding one.}

\item{n_sel}{Hybrids selected per cycle.}

\item{alpha_max}{Diversity budget. `Inf` leaves the selection unconstrained.}

\item{n_qtl, fst, h2}{Passed to [simulate_pools] for the founding population.}

\item{max_use}{Optional per-line usage cap, passed to the selector.}

\item{selector}{Selection function of `(ctx, n_sel, alpha_max, gd_ref,
max_use)` returning indices. Defaults to a greedy sweep, which is cheap and
enough to establish a trend; pass a `sel_de` wrapper to confirm one.}

\item{inject}{Optional `list(cycle =, n =)`: at that cycle, replace `n` lines
of each pool with lines drawn from the founding generation.}

\item{B_null}{Resampling replicates for the per-cycle random baseline.}

\item{seed}{Seed for the founding population and the recycling draws.}
}
\value{
A data frame, one row per cycle, with the realised index (on the
  founding cycle's scale), gene diversity, the realised `alpha`, both
  inbreeding lenses against the frozen `p0`, the pool partition, and the
  maximum line usage.
}
\description{
One cycle is: form every A x B hybrid, select `n_sel` of them under a gene
diversity budget, read off how much each parent line was used, and rebuild
each pool by intermating its own lines in proportion to that usage. Marker
effects, and the base frequencies `p0` that anchor `F_hom` and `F_drift`, are
fixed at cycle 0 and carried forward unchanged.
}
\seealso{
[next_pool] for the recycling step, [line_weights] for the usage
  vector that drives it.
}
