% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/08_mabc.R
\name{run_bc_program}
\alias{run_bc_program}
\title{Run one backcross programme}
\usage{
run_bc_program(n_sched, s, strategy, map, opt, tg, w, shared = NULL)
}
\arguments{
\item{n_sched}{Plants per backcross generation, e.g. `c(120, 120, 120)`.
Its length is the number of backcrosses.}

\item{s}{Fraction of the genome shared by donor and elite.}

\item{strategy}{Selection policy, see [select_parent].}

\item{map}{A map from [build_map].}

\item{opt}{Options, see [mabc_opt].}

\item{tg}{Target marker indices, see [target_indices].}

\item{w}{Marker weights, see [marker_weights].}

\item{shared}{Optional shared-genome mask. Pass the same mask across
strategies to compare them on common random numbers.}
}
\value{
A list with `traj` (one row per generation), `Hfinal`, `shared` and
  `snap` (the selected haplotype of each generation).
}
\description{
One selected plant is advanced per generation, so every probability derived
from this function is a **single-lineage** probability. Real programmes
advance 3-10 plants, which both understates achievable progress and removes
lineage-level variance.
}
