% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_search.R
\name{sel_de_fast}
\alias{sel_de_fast}
\title{Differential evolution on the line encoding}
\usage{
sel_de_fast(
  ctx,
  n_sel,
  gd_ref,
  alpha_max,
  ne_par_min = NULL,
  max_use = NULL,
  pool_tol = NULL,
  NP = 300,
  itermax = 2000,
  seeds = NULL,
  trace = FALSE
)
}
\arguments{
\item{ctx}{Context augmented by [fast_ctx].}

\item{n_sel}{Number of hybrids to select.}

\item{gd_ref}{Reference gene diversity from [null_distribution].}

\item{alpha_max}{Maximum acceptable relative loss of gene diversity.}

\item{ne_par_min}{Minimum effective number of parents, or `NULL`.}

\item{max_use}{Maximum use of any single line, or `NULL`.}

\item{pool_tol}{Tolerance on the deviation of pool balance from 0.5, or
`NULL`.}

\item{NP}{Population size.}

\item{itermax}{Maximum generations.}

\item{seeds}{A list of line-weight vectors to seed the population. `NULL`
builds the diversity sweep; it never means "start cold".}

\item{trace}{Passed to `DEoptim`.}
}
\value{
An integer vector of selected hybrid indices.
}
\description{
Drives `DEoptim` on [decode_lines] and [make_fitness_fast], the pair the
package has exported since the line route was written but never had a driver
for. The search dimension is `L` rather than `N` -- 200 instead of 10000 at
production scale -- and no `N x N` matrix is formed. At an equal
60,000-evaluation budget under four simultaneous restrictions this reaches
index 1.988 against 0.351 for the hybrid encoding of [sel_de].
}
\details{
Warm start seeds sweep a line-diversity direction: `p` proportional to the
negated mean coancestry of each line, at increasing strength. At strength
zero the decode is capped truncation, which is the same starting point
[sel_de]'s `w = 0` greedy seed provides.
}
\seealso{
Other selection strategies:
\code{\link[=ocs_round]{ocs_round()}},
\code{\link[=sel_de]{sel_de()}},
\code{\link[=sel_exact]{sel_exact()}},
\code{\link[=sel_greedy]{sel_greedy()}},
\code{\link[=sel_local_search]{sel_local_search()}},
\code{\link[=sel_random]{sel_random()}},
\code{\link[=sel_truncation]{sel_truncation()}},
\code{\link[=sel_truncation_cap]{sel_truncation_cap()}}
}
\concept{selection strategies}
