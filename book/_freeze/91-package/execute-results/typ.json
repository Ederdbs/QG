{
  "hash": "87e8238b809b0707328b54c72abaaa91",
  "result": {
    "engine": "knitr",
    "markdown": "# The `hybdiv` package {#sec-package}\n\n\n\nEvery function used in this book lives in one package, whose source is the `R/`\ndirectory of this repository. This appendix is the index.\n\n\n::: {.cell layout-align=\"center\"}\n\n```\n#> exported_objects \n#>              193\n```\n:::\n\n\n## Installation\n\n\n::: {.cell layout-align=\"center\"}\n\n```{.r .cell-code}\ninstall.packages(c(\"DEoptim\", \"quadprog\"))\ndevtools::install(\".\")        # from the repository root\nlibrary(hybdiv)\n```\n:::\n\n\nOnly `DEoptim` is a hard dependency, used solely by `sel_de()`. `quadprog` is\nsuggested and needed only for `optimal_contributions()` and `ocs_quadprog()`.\nEverything else, including every plot, is base R.\n\n## File map\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|File                      |Role                                                              |Chapter                 |\n|:-------------------------|:-----------------------------------------------------------------|:-----------------------|\n|`R/00_data.R`             |simulation, the two matrices, the `ctx` builder                   |@sec-simulating-crosses |\n|`R/01_metrics.R`          |the diversity metrics, all `f(idx, ctx) -> scalar`                |@sec-catalogue          |\n|`R/02_benchmark.R`        |null distribution, discriminatory power, cost, plots              |@sec-catalogue          |\n|`R/03_de_select.R`        |selection strategies: truncation, greedy, DE, OCS relaxation      |@sec-combinatorial      |\n|`R/04_fast_metrics.R`     |the line-level fast route -- no N x N matrix                      |@sec-choosing           |\n|`R/05_pipeline_metrics.R` |stage-specific metrics for S1 to S4                               |@sec-stages             |\n|`R/06_f2size.R`           |F1 to F4 population sizing simulator                              |@sec-sizing             |\n|`R/07_caballero_toro.R`   |Caballero & Toro subdivided-population analysis                   |@sec-subdivided         |\n|`R/08_mabc.R`             |marker-assisted backcrossing: drag, the selection index, sizing   |@sec-backcross          |\n|`R/09_reference.R`        |slow literal oracles, for verification                            |@sec-verification       |\n|`R/10_stage1.R`           |stage 1: lines to the X / f / hybrids contract                    |@sec-simulating-crosses |\n|`R/11_stage2.R`           |stage 2: constrained selection across an alpha grid               |@sec-case-study         |\n|`R/12_search.R`           |structure-aware search: local search, the bound, the exact oracle |@sec-advanced           |\n|`R/13_dominance.R`        |heterosis under a dominance model, and its link to the partition  |@sec-heterosis          |\n|`R/14_cycles.R`           |recurrent selection across cycles                                 |@sec-cycles             |\n|`R/15_response.R`         |genic variance, and the Smith-Hazel and restricted indices        |@sec-response           |\n\n\n:::\n:::\n\n\nFiles are numbered because the package has no `Collate` field and sourcing order\nis alphabetical.\n\n## The two-stage contract\n\nEverything downstream of stage 1 talks only through three objects. This is the\nseam for swapping in real data.\n\n\n::: {.cell layout-align=\"center\"}\n\n```{.r .cell-code}\nst1 <- stage1_simulate()                 # or stage1_build(X, ped, traits, fL)\n#   $X        N x m hybrid markers, 0/0.5/1\n#   $f        N x N molecular coancestry\n#   $hybrids  N x (3 + n_traits): id, line_A, line_B, traits\n#   $fL       L x L line coancestry (optional)\n\nres <- stage2_select(st1$X, st1$f, st1$hybrids, n_sel = 100, fL = st1$fL)\n#   $selection  0/1 column per scenario, plus n_scenarios\n#   $metrics    one row per scenario\n#   $z_scores   each metric in s.d. from the random null\n#   $ref        gd_ref, its Monte Carlo s.e., alpha_max, n_sel\n```\n:::\n\n\n## Function index\n\n### Simulation and context\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                               |Does                                                          |\n|:--------------------------------------|:-------------------------------------------------------------|\n|`sim_config`                           |simulation scale: lines per pool, markers, traits, divergence |\n|`simulate_lines()`                     |Balding-Nichols line genotypes in two divergent pools         |\n|`simulate_data()`                      |lines, all A x B crosses, both matrices, predicted traits     |\n|`simulate_pools()`                     |lighter simulator returning raw pieces, with `GL` in 0/1      |\n|`build_ctx()`                          |assemble the context; freezes `p0` and the index              |\n|`build_ctx_from_stage1()`              |same, from the stage-1 contract                               |\n|`ctx_from_pools()`                     |context from `simulate_pools()`, carrying `parents` and `GL`  |\n|`load_data()`                          |the single swap point for real data                           |\n|`stage1_simulate()` / `stage1_build()` |produce the stage-1 contract                                  |\n|`stage2_select()`                      |the whole constrained selection pipeline                      |\n\n\n:::\n:::\n\n\n### The two matrices\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                                 |Does                                                        |\n|:----------------------------------------|:-----------------------------------------------------------|\n|`molecular_coancestry()`                 |**the diversity kernel**; one `tcrossprod`, stays in [0, 1] |\n|`vanraden_G()`                           |**prediction only**; `sum(G) == 0` by construction          |\n|`mrd_matrix()` / `coancestry_from_mrd()` |modified Rogers distance, the same statistic                |\n\n\n:::\n:::\n\n\n### Diversity metrics -- all `f(idx, ctx) -> scalar`\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                             |Does                                                              |\n|:------------------------------------|:-----------------------------------------------------------------|\n|`theta_group()`                      |group coancestry, diagonal included                               |\n|`gene_diversity()` / `he_nei()`      |`1 - theta`; the same quantity, two routes                        |\n|`theta_from_freq()`                  |theta via allele frequencies -- validation only, ~100x slower     |\n|`status_number()`                    |`Ns = 1/(2 theta)`; a static descriptor                           |\n|`ne_parents()`                       |effective number of parent lines; independent of `f`              |\n|`ne_lines_pool()`                    |the same, per pool; needs only the pedigree                       |\n|`alleles_lost()`                     |markers polymorphic in the pool, rare in the selection            |\n|`freq_metrics()`                     |**one marker sweep**: He, F_hom, F_drift, cov_diag, rare_retained |\n|`line_weights()` / `theta_pools()`   |per-pool usage weights and coancestries                           |\n|`gd_partition()`                     |Caballero & Toro Eq. 8 over the heterotic pools                   |\n|`ene()` / `ane()`                    |Core Hunter entry- and accession-to-nearest-entry                 |\n|`eff_dim()`                          |effective dimensionality, on the **double-centered** submatrix    |\n|`gst()`                              |Nei's Gst, selected against non-selected                          |\n|`mean_offdiag()`                     |the common production metric, kept for comparison                 |\n|`metrics_cheap()` / `metrics_full()` |the inner-loop panel and the post-hoc panel                       |\n\n\n:::\n:::\n\n\n### The fast line route\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                                                    |Does                                                   |\n|:-----------------------------------------------------------|:------------------------------------------------------|\n|`fast_ctx()`                                                |add `parents`, `GL` and the packed bitset to a context |\n|`line_counts()` / `line_usage_freq()`                       |the sufficient statistic                               |\n|`fast_theta()` / `fast_gene_div()` / `fast_status_num()`    |`theta = w' fL w`; no N x N matrix                     |\n|`make_theta_fun()`                                          |precompiled closure for ~1e6 evaluations               |\n|`theta_state()` / `theta_swap()`                            |O(L) incremental update per swap                       |\n|`fast_ne_parents()` / `ne_parents_from_counts()`            |free once the counts exist                             |\n|`fast_pbar()` / `fast_he_nei()` / `fast_alleles_lost()`     |frequencies from the line matrix                       |\n|`pack_lines()` / `popcount32()` / `alleles_retained_fast()` |bitset allelic richness                                |\n|`fast_theta_pools()`                                        |pool decomposition from the same counts                |\n|`decode_rank()` / `decode_lines()`                          |the two encodings; **use `decode_lines()`**            |\n|`make_fitness_fast()`                                       |DE fitness with several simultaneous restrictions      |\n\n\n:::\n:::\n\n\n### Selection strategies\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                                        |Does                                                        |\n|:-----------------------------------------------|:-----------------------------------------------------------|\n|`sel_random()`                                  |the null strategy                                           |\n|`sel_truncation()`                              |ceiling on gain, floor on diversity                         |\n|`sel_truncation_cap()`                          |per-line cap; no matrix needed, usually most of the benefit |\n|`sel_greedy()`                                  |incremental greedy; `w = 0` is max diversity                |\n|`decode()` / `make_fitness()` / `sel_de()`      |differential evolution, warm-started by default             |\n|`project_capped_simplex()` / `ocs_relaxation()` |the continuous upper bound                                  |\n|`optimal_contributions()` / `ocs_quadprog()`    |quadratic-programming OCS                                   |\n\n\n:::\n:::\n\n\n### Benchmarking\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                                                 |Does                                                                       |\n|:--------------------------------------------------------|:--------------------------------------------------------------------------|\n|`null_distribution()`                                    |**the correct baseline**: random subsets of the same size                  |\n|`alpha_loss()`                                           |proportional diversity loss against that baseline                          |\n|`evaluate()` / `discriminatory_power()`                  |metric panel and its z-scores                                              |\n|`cost_per_eval()`                                        |microseconds per metric; decides what goes inside the loop                 |\n|`plot_null()` / `plot_correlation()` / `plot_frontier()` |base-R diagnostics; run by `inst/scripts/run_benchmark.R`, not by the book |\n\n\n:::\n:::\n\n\n## Runnable scripts\n\nThe book runs everything at a reduced scale so it renders quickly. Three scripts\nship with the package and run the same work at the production scale of\n`sim_config`, writing their tables and plots to `report/`:\n\n```sh\nRscript inst/scripts/run_all.R          # the two-stage pipeline, ~4 min\nRscript inst/scripts/run_benchmark.R    # the metrics benchmark, ~6 min\nRscript inst/scripts/run_mabc.R         # the MABC scans, ~3 min\n```\n\n`run_benchmark.R` is the source of the numeric results quoted in `README.md`,\nand the only place the three `plot_*` helpers above are exercised. From an\ninstalled copy of the package:\n\n\n::: {.cell layout-align=\"center\"}\n\n```{.r .cell-code}\nsource(system.file(\"scripts\", \"run_all.R\", package = \"hybdiv\"))\n```\n:::\n\n\n### Pipeline stages S1-S4\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                                                            |Does                                                   |\n|:-------------------------------------------------------------------|:------------------------------------------------------|\n|`theta_decompose()` / `gst_nei()`                                   |**both** F_ST definitions, and Nei's Gst               |\n|`pool_freqs()`                                                      |allele frequencies per pool, one row each              |\n|`pool_complementarity()` / `s1_monitor()`                           |the S1 cycle panel; `mean_sq_delta_p` is the exact one |\n|`dh_sib_coancestry()` / `dh_cross_coancestry()` / `dh_pool_theta()` |the three DH identities                                |\n|`sigma2_dh()` / `sigma_dh_crosses()`                                |progeny variance from marker effects                   |\n|`usefulness_criterion()` / `sel_intensity()`                        |UC -- applies at S2, not S4                            |\n|`s3_theta()` / `alleles_retained()`                                 |line-level metrics                                     |\n|`loss_vs_resampling()` / `theta_ceiling()`                          |calibrate against a same-size baseline                 |\n|`select_lines_constrained()`                                        |truncate-then-repair; the incremental greedy fails     |\n|`hybrid_theta_from_usage()` / `line_usage()` / `pool_balance()`     |S4 on the line route                                   |\n|`loss_ledger()`                                                     |loss per stage, on one scale                           |\n\n\n:::\n:::\n\n\n### Heterosis and cycles\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                |Does                                                          |\n|:-----------------------|:-------------------------------------------------------------|\n|`heterosis_value()`     |mid-parent heterosis of each hybrid, from `d` alone           |\n|`heterosis_expected()`  |the same, expected over a pool pair                           |\n|`heterosis_partition()` |shared and divergence terms -- `GD_T` and `GD_BS`             |\n|`run_cycles()`          |recurrent selection, one row per cycle                        |\n|`next_pool()`           |one generation of a pool: weighted intermating, DH derivation |\n|`sel_greedy_budget()`   |greedy sweep returning the best plan inside a budget          |\n\n\n:::\n:::\n\n\n### Caballero & Toro\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function         |Does                                                          |\n|:----------------|:-------------------------------------------------------------|\n|`ct_partition()` |subpopulation coancestries, F statistics, the Eq. 7 partition |\n|`ct_loss_gain()` |loss **or gain** from removing each subpopulation             |\n\n\n:::\n:::\n\n\n### Population sizing\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                                                           |Does                                                        |\n|:------------------------------------------------------------------|:-----------------------------------------------------------|\n|`maize_chr_len` / `f2_distorters` / `f2_opt`                       |defaults; **replace the distorters** with your data         |\n|`build_map()` / `marker_index()`                                   |Haldane map on a regular grid                               |\n|`meiosis()` / `meiosis_sel()` / `cross_indices()` / `make_F1()`    |the vectorised engine                                       |\n|`make_distortion()` / `zyg_fitness()`                              |gametophytic and zygotic distortion, by exact rejection     |\n|`run_scheme()` / `reps_for_N()` / `bulk_weights()`                 |SSD, bulk, Syn schemes                                      |\n|`panel_freq()` / `panel_het()` / `diversity_retained()`            |panel metrics                                               |\n|`min_minor_freq()` / `fixed_fraction()` / `skew_fraction()`        |worst-region metrics                                        |\n|`n_equivalent()`                                                   |equivalent N -- a communication device, **not** a Wright Ne |\n|`block_lengths()` / `junctions_per_hap()` / `expected_junctions()` |map expansion                                               |\n|`multilocus_recovery()` / `f1_sizing()`                            |the objectives that keep paying for larger N                |\n|`f2_run_experiment()`                                              |the full grid; minutes                                      |\n\n\n:::\n:::\n\n\n### Reference oracles\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|Function                                                                            |Does                   |\n|:-----------------------------------------------------------------------------------|:----------------------|\n|`ref_theta()`, `ref_gene_div()`, `ref_theta_freq()`, `ref_he_nei()`                 |literal transcriptions |\n|`ref_status_num()`, `ref_ne_parents()`, `ref_alleles_lost()`, `ref_alleles_fixed()` |-                      |\n|`ref_ene()`, `ref_ane()`, `ref_eff_dim()`, `ref_theta_pools()`                      |-                      |\n\n\n:::\n:::\n\n\n## Full export list\n\n\n::: {.cell layout-align=\"center\"}\n::: {.cell-output-display}\n\n\n|                        |                        |                          |                         |\n|:-----------------------|:-----------------------|:-------------------------|:------------------------|\n|alleles_lost            |fast_alleles_lost       |mean_offdiag              |reps_for_N               |\n|alleles_retained        |fast_ctx                |meiosis                   |rrs_config               |\n|alleles_retained_fast   |fast_gene_div           |meiosis_sel               |run_bc_program           |\n|alpha_loss              |fast_he_nei             |metrics_cheap             |run_cycles               |\n|alphasimr_founder_pools |fast_ne_parents         |metrics_full              |run_scheme               |\n|alphasimr_gd            |fast_pbar               |min_minor_freq            |s1_monitor               |\n|alphasimr_make_hybrids  |fast_status_num         |molecular_coancestry      |s3_status_num            |\n|alphasimr_pipeline      |fast_theta              |mrd_matrix                |s3_theta                 |\n|alphasimr_rrs_cycle     |fast_theta_pools        |multilocus_recovery       |sel_de                   |\n|alphasimr_to_stage1     |fixed_fraction          |n_equivalent              |sel_de_fast              |\n|ane                     |foreground_ok           |n_for_count               |sel_exact                |\n|backcross               |freq_metrics            |ne_lines_pool             |sel_greedy               |\n|BITS_PER_WORD           |gd_partition            |ne_parents                |sel_greedy_budget        |\n|block_lengths           |gene_diversity          |ne_parents_from_counts    |sel_intensity            |\n|build_ctx               |genic_var               |next_pool                 |sel_local_search         |\n|build_ctx_from_stage1   |gst                     |null_distribution         |sel_random               |\n|build_map               |gst_nei                 |ocs_bound                 |sel_truncation           |\n|bulk_weights            |haldane_r               |ocs_quadprog              |sel_truncation_cap       |\n|coancestry_from_mrd     |he_nei                  |ocs_relaxation            |select_lines_constrained |\n|col_cumsum              |heterosis_expected      |ocs_round                 |select_parent            |\n|cost_per_eval           |heterosis_gca_sca       |optimal_contributions     |selfing_step             |\n|cp_hi                   |heterosis_partition     |p_het                     |sigma_dh_crosses         |\n|cp_lo                   |heterosis_value         |p_same_segment            |sigma2_dh                |\n|cross_indices           |hybrid_theta_from_usage |pack_lines                |sim_config               |\n|ct_loss_gain            |ibd_elite               |panel_freq                |simulate_data            |\n|ct_partition            |inbreeding_depression   |panel_het                 |simulate_lines           |\n|ctx_from_pools          |index_restricted        |plot_correlation          |simulate_pools           |\n|decode                  |index_smith_hazel       |plot_frontier             |sizing_table             |\n|decode_lines            |junctions_per_hap       |plot_null                 |skew_fraction            |\n|decode_rank             |line_counts             |pool_balance              |stage1_build             |\n|dh_cross_coancestry     |line_usage              |pool_complementarity      |stage1_simulate          |\n|dh_pool_theta           |line_usage_freq         |pool_freqs                |stage2_select            |\n|dh_sib_coancestry       |line_weights            |popcount32                |status_number            |\n|discriminatory_power    |load_data               |project_capped_simplex    |strat_rec_until          |\n|diversity_retained      |loss_ledger             |ref_alleles_fixed         |summarise_reps           |\n|donor_dosage            |loss_vs_resampling      |ref_alleles_lost          |target_indices           |\n|donor_specific          |mabc_opt                |ref_ane                   |theta_ceiling            |\n|drag_total              |maize_chr_len           |ref_eff_dim               |theta_decompose          |\n|eff_dim                 |maize_targets           |ref_ene                   |theta_from_freq          |\n|ene                     |make_distortion         |ref_gene_div              |theta_group              |\n|evaluate                |make_F1                 |ref_genic_var             |theta_pools              |\n|expected_donor          |make_fitness            |ref_he_nei                |theta_state              |\n|expected_drag           |make_fitness_fast       |ref_heterosis             |theta_swap               |\n|expected_ibd            |make_shared_ibd         |ref_inbreeding_depression |usefulness_criterion     |\n|expected_junctions      |make_theta_fun          |ref_ne_parents            |vanraden_G               |\n|f1_sizing               |marker_index            |ref_status_num            |zyg_fitness              |\n|f2_distorters           |marker_weights          |ref_theta                 |                         |\n|f2_opt                  |max_line_use            |ref_theta_freq            |                         |\n|f2_run_experiment       |mean_index              |ref_theta_pools           |                         |\n\n\n:::\n:::\n\n",
    "supporting": [
      "91-package_files"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {
      "knitr": [
        "{\"type\":\"list\",\"attributes\":{},\"value\":[]}"
      ]
    },
    "preserve": null,
    "postProcess": false
  }
}